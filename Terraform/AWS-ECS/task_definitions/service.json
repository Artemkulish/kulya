[
    {
      "name": "nodejs_application",
      "image": "${docker_image}",
      "cpu": ${cpu},
      "memory": ${memory},
      "networkMode": "awsvpc",
      "portMappings": [
        {
          "containerPort": ${https},
          "hostPort": ${https}
        }
      ]
    }
  ]